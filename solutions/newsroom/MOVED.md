# ⚠️ Moved — this is a frozen copy

**The Newsroom (BrightPath Intelligence) now lives in its own repository.**
Do all future work there — this `solutions/newsroom/web` copy is a frozen
snapshot kept for history only. Editing it will cause the two to diverge.

- **Canonical repo:** https://github.com/victorycross/Brightpath_Intelligence
- **Local:** `~/Projects/Brightpath_Intelligence/` (app at the repo root, flattened)
- **Current state / hand-off:** see `docs/HANDOFF.md` in that repo.

Frozen 2026-06-14 at parity with the standalone repo (Supabase migrations
0001–0009, all applied to the live `newsroom` project). The live deployment at
`newsroom.brightpathtechnology.io` is served from the standalone repo's Vercel
project.
