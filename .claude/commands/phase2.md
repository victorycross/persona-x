Read `docs/PHASE2-WORKPLAN.md` and `CLAUDE.md` to understand the current project state and Phase 2 priorities.

Then:
1. Run `/status` to verify the build is healthy
2. Check git status for any uncommitted work
3. Report which Phase 2 task is next based on the workplan
4. Ask what I want to work on

Do NOT start coding until you have read the workplan and confirmed the build is green.
